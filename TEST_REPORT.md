# 展示会注文管理 UI改善 テストレポート

実施日: 2026-08-24
対象: 通常版 `index.html` / 単一HTML版 `exhibition_order_production_preview.html`

## 結果

- 自動テスト: **42 / 42 PASS**（既存24件を維持し18件追加）
- `workflow.js` 構文チェック: PASS
- `app.js` 構文チェック: PASS
- 単一HTML再生成: PASS
- 指定3画面幅の横はみ出し: なし
- 新規コンソールエラー: なし
- 未同期注文を含む最終提出の停止: PASS

実行コマンド:

```powershell
node --test tests/*.test.mjs
node --check workflow.js
node --check app.js
node scripts/build-preview.mjs
```

## 自動回帰

次を含む42件が合格しました。

- 商品マスター3,861件、改定価格、価格未定2件の注文禁止、重複品番の個別保持
- 本社未共有・受取待ち・未会計の横断抽出と、件数カード・一覧件数の一致
- 店舗、受付番号、電話、品番、商品名の横断検索
- 状態変更後の検索・絞り込み解除と移動先表示
- 下書き再開、破棄キャンセル、破棄確定
- 現売り渡し方の初期未選択、ホテル配送3、指定住所配送4
- その他卸屋名の必須検査、具体名保存、候補外名称の復元構造
- 海外電話番号の許容と想定外文字の警告
- 日本時間23:59 / 00:01の受付日境界
- 本日、指定期間（開始・終了を含む）、全期間の件数・点数・金額
- 日付不明の注文を全期間だけへ含める処理
- 未同期がある最終提出の停止
- QR失敗時の同一注文再試行、`clientSubmissionId` によるサーバー側重複防止
- 修正時の `localId`、`createdAt`、`clientSubmissionId`、`publicToken` 維持
- お客様控えから状態、本社共有、受け渡し、作成日時、社内担当、卸屋、社内備考を除外
- 本番ログイン画面から匿名端末モード入口を除外
- 320px向け40px固定キー、44px以上の補助操作、48px主要ボタン

## ブラウザ実操作

環境: Codex In-app Browser / ローカルHTTPサーバー
本番データを作成しない `127.0.0.1` 限定の内部確認経路を使用しました。

### 本番入口

- `onlineEnabled: true` の通常URLで登録スタッフのログイン画面を表示: PASS
- 匿名の端末モードボタンが存在しない: PASS
- メール・パスワード未入力で注文管理へ入る経路がない: PASS
- 本番アカウントでの実ログイン: 未実施（認証情報未提供、本番送信を回避）

### 一覧・検索・状態

- `未会計 2` を押し、受取待ち1件と完了1件の合計2カードを表示: PASS
- 絞り込み中は選択中タブを0個にし、`絞り込み結果（全フォルダ）` を表示: PASS
- `ロープレ` 検索で受取待ち・完了の2フォルダから2件を表示: PASS
- 検索中は `検索結果（全フォルダ）` を表示し、タブ選択を解除: PASS
- 完了タブを押すと検索語と検索結果表示を解除: PASS
- 新規の後日受取注文を要対応へ登録: PASS
- 本社共有済みにすると受取待ちへ移り、移動直後に対象カードを表示: PASS
- 会計・お渡し完了にすると完了へ移り、移動直後に対象カードを表示: PASS
- 完了カードの詳細操作を1つに統一: PASS

### 入力・下書き

- 商品0点で `注文内容へ進む` が無効: PASS
- 固定数字キー14個がすべて40px高、`英字` 表記: PASS
- 商品追加後に進む操作が有効: PASS
- 下書きを閉じ、同じ商品・検索内容で再開: PASS
- 破棄確認で商品1点を表示し、キャンセル後に下書きを復元: PASS
- 破棄確定後、商品0点の新規注文を開始: PASS
- 現売り選択直後は渡し方0件選択、`入力へ進む` が無効: PASS
- 渡し方1〜4を表示: PASS
- ホテル配送でホテル・宿泊者・部屋・チェックアウト欄を表示: PASS
- 指定住所配送で住所欄だけを表示: PASS
- その他選択時だけ具体名欄を表示: PASS
- `+81 (0)3 ABC` は警告、`+81 (0)3 1234-5678` は警告解除: PASS

### QR

- ローカル保存時に `注文は保存済み・QRは未発行` を明示: PASS
- 失敗画面に注文番号、店舗、保存済みを表示: PASS
- `QRをもう一度作る` を2回操作しても対象注文は1件のまま: PASS
- ローカルテスト用公開トークンで `QR準備完了` とQR画像・canvasを生成: PASS
- 注文修正後も同じ保存済み注文からQRを再表示: PASS
- 実Supabaseでの公開トークン新規発行、別端末読み取り、控え画像長押し保存: 未実施（本番認証情報を使わず、外部注文作成を行わないため）

QR再試行は新規保存処理を呼ばず、保存済み注文の `localId` から同じオブジェクトを取得します。サーバー側も `clientSubmissionId` の既存行を返すため、応答喪失後の再試行でも二重登録を防ぎます。

### 提出・印刷

ブラウザ内にあったローカル確認データで次を照合しました。

- 本日: 3件 / 3点 / 33,300円 / 未同期3件
- 指定日 2026-08-24〜2026-08-24: 同じ3件 / 3点 / 33,300円
- 全期間: 7件 / 7点 / 77,700円 / 未同期7件
- 最終提出: 未同期ありのため出力ボタン無効: PASS
- 国内通常注文（本日2件）: 未同期警告を表示しつつ伝票打ち用出力は有効: PASS
- 個別注文の印刷ボタン: 一括提出制御の外に維持
- 表紙の対象受付日、展示会名、出力日時、件数、点数、合計と、一覧の受付日時: 自動回帰で確認

### 画面幅

- 390×844: `scrollWidth <= innerWidth`、QR画面を含め横はみ出しなし
- 320×568: フォーム・商品画面とも横はみ出しなし。固定キー40px、主要ボタン48px
- 1024×768: アプリ幅760pxで中央配置、固定新規注文ボタンも同じ幅内
- 通常版と単一HTML版: タブ件数・重要件数が一致し、どちらも新規コンソールエラーなし

## 実装しなかったもの

- 個人情報や改ざん対策を満たす根拠のない緊急用オフラインQRは追加していません。ログイン済み注文を端末保存し、接続復旧後に同じ注文から公開トークンを取得する方式です。
- 本番スタッフ認証、実Supabase同期、別スマートフォンでのQR読み取り・画像長押し保存は、認証情報と本番注文作成を伴うため今回のローカル検証では実施していません。

## 本番前の人手確認

1. 登録スタッフの実アカウントでログインし、未同期0件まで同期されること。
2. 通常注文・その場渡し・後日受取を各1件、本番相当の検証イベントで登録すること。
3. 発行QRを別スマートフォンで読み取り、控え画像を長押し保存すること。
4. 注文修正後、同じQR URLで最新内容が表示されること。
5. 23:59 / 00:01の実時刻または検証データで最終PDFを照合すること。
6. 責任者が本社未共有・要対応・受取待ち警告の運用を確認すること。
