# 後日受取・お渡し完了・Slack共有・印刷 テストレポート

最終追記: 2026-09-17（以前の検証記録も含む）

## 結果

- 自動テスト: **101 / 101 PASS**
- `app.js`、`workflow.js`、`security.js`、`receipt-share.js`: 構文チェックPASS
- 安全な公開ビルド: PASS（11ファイル、商品CSV・Excel・非公開画像なし）
- 実Supabase: 新注文テーブルのRLS有効、匿名SELECT権限なし、スタッフの物理DELETE権限なし
- 公開キーだけで実商品・注文APIへアクセス: 両方HTTP 401
- 旧注文13件を移行し、会社名・電話・明細件数・合計金額・受付日が元データと一致
- 旧注文22件は維持、DB検証用注文の残存0件
- PC・スマホの横はみ出し: なし
- ブラウザ実操作中のJavaScriptエラー: 0件
- A4注文書: 単票1ページ、共有ボタン出力1ページ、一括3ページ（一覧＋通常注文＋後日受取）、欠け・重なり・文字化けなし
- 後日受取の会計フロー: 受け取り時会計の自動選択、会計方法の記録、会計前のお渡し防止、二重操作・競合・通信失敗の保護を確認
- PDF保存時の初期ファイル名: お客様名・日本時間の日付と時刻を含み、使用不可文字を安全に置換することを確認

## 自動回帰

2026-09-17の写真添付は `tests/photo-attachments-browser-check.mjs` でChrome系・WebKit系を確認しました。

- 写真フォルダの複数選択、撮影用の単一選択と背面カメラ指定、プレビュー・削除、削除時の画像URL解放
- 非画像・壊れた画像のエラー表示と再操作、6枚の上限、20MBのファイル制限（単体テスト）
- 写真の追加では外部へ書き込まず、注文更新の送信内容にも写真・画像URL・写真名を含まない
- 画像の読み込み完了後に印刷。共有確認後は写真をロックし、変更後はPDFの再作成が必要
- 新しい注文への写真混入なし、ログアウト後の画像解放、遅い画像処理完了時の復活防止（単体テスト）
- 320／390／1280pxで写真操作部の横はみ出しなし
- 実際のA4 PDFを出力して3ページ（注文書・縦写真・横写真）を画像化し、すべて目視確認。写真の切れ・変形・重なり・不要な空白ページなし
- お客様向けQRと会社添付資料を分離。実機でのカメラ起動・撮影、端末固有のHEIC読込み、物理プリンターは未実施

2026-09-16の番号発行では、参考アプリに合わせて入力画面から1回で一時保存・NEO番号表示へ進む操作を検証しました。`tests/number-issue-browser-check.mjs` は架空データだけを使用し、Chrome系・WebKit系で以下を確認します。

- 必須項目の不足では採番せず、通信失敗時は番号を表示しない。再試行・連打でも注文を二重登録しない
- 明日・明後日の受取日、入力画面の番号発行、発行後の修正・再読み込み・別端末で同じ番号を保持
- 異なる端末からの別注文には異なる番号を発行
- 番号発行・印刷だけでは未確定を維持し、送信確認後の確定で受け取り待ちへ移動
- 同じ番号が注文書とお客様向けQR画面に出ること、署名付き控えPNGを未ログインで取得できること
- 320／390／768／1280pxで入力・発行後画面に横はみ出しなし。320pxでも番号発行ボタンは1行で高さ48px以上
- 通常・その場渡し・ホテル配送・指定住所配送を含む5区分の保存、未確定を除く一括印刷

DBの確定状態管理は、本番の既存注文を変更しない架空ホテル・通常注文を使ったトランザクションで検証し、最後にROLLBACKしました。採番シーケンスを消費する新規後日受取はDBへ作成していません。以下の既存PDFの目視検証は前回までの記録です。

2026-09-15追加検証:

- 海外のお客様控えのみ英語化。国内・区分未設定の控えと社内用注文書は日本語を維持。商品名・店舗名・氏名は登録どおり、社内備考・スタッフ・帳合先は非表示。
- NEO番号、現金での会計済み・受け取り時会計、送料500円、商品点数・合計、入力文字のHTMLエスケープを自動検証。
- 控え印刷の本文・日時・ファイル名の英語切替と、言語変更時に画像キャッシュが別になることを自動検証。
- ローカルの架空注文で海外を選び、実際の控えPNG（1800×1206）を生成して目視確認。Pickup No. NEO-1、Payment on pickup、Shipping (flat rate)、商品1点・合計600円を確認し、欠け・重なりなし。本番顧客データは使用していない。

2026-09-14追加検証:

- ホテル名・宿泊者名・配送先住所を空欄のまま確定できる。架空データのホテル配送で実操作確認。
- 送料500円の追加・取消・二重追加防止。合計とクラウド往復に含め、商品点数からは除外。送料だけの注文は不可。
- NEO番号はDB専用列からのみ復元。端末で付けた番号を送信しない。受付番号は維持。翌日・翌々日受取だけ表示し、番号検索できる。
- DBの変更と51件の採番検証を1トランザクションで実施後、全てROLLBACK。重複なし、会計・渡し方変更・取消時の番号維持、スタッフの番号上書き・採番リセット禁止、ホテル配送時は未採番・後日受取への変更時に採番することを確認。
- 架空注文の実際のお客様控えPNGを生成し、NEO-1が受付番号と別枠で大きく表示されることを目視確認。番号発行前の後日受取の印刷は止め、注文確定後の出力を案内する。
- 本番反映後、既存10注文を維持し、後日受取3件にNEO-1〜NEO-3を付与。未採番・重複0件。スタッフの番号上書き・カウンタ初期化権限なし、匿名の採番権限なし。追加した機能へのセキュリティ警告なし（従来の別機能への警告は継続）。

以下は従来機能の検証記録。

59件には次を含みます。旧データの状態判定に関するテストは後方互換性の確認です。通常注文・その場渡しは確定で完了、ホテル・指定住所配送は共有済みで完了、後日受取は共有済みで受け取り待ち・お渡し済みで完了になります。

- 未認証画面・Supabase認証・旧匿名注文APIを再開しないこと
- セッションの端末保持、更新トークン更新、ログアウト
- 商品のページ取得、商品・帳合先・注文のRLSと列権限
- クラウド保存の確認、再試行の重複防止、同時編集の競合検知
- 注文の送信項目制限（セッション・画面状態を含めない）
- サーバーID・日時を使った注文復元
- 印刷後の注文保持、ログアウト時のメモリ消去
- 注文・顧客情報をlocalStorageへ保存しない構造
- 別スタッフの注文を修正しても担当者名を保持
- 要対応／受け取り待ち／完了の3タブ、状態変更、全状態を横断する検索
- 控え画像の安全なパス・署名URL、画像専用の非公開Storage RLS
- 品番検索・固定入力キー・数量変更
- 通常注文と渡し方4種の入力検証
- 日付別一括印刷、区分・帳合先順の整列
- 公開ビルドの許可リスト、非公開ファイルの追跡除外
- Slack共有チェック・解除による状態遷移、確認日時の保持、同期項目の追加
- 後日受取のお渡し判定、旧共有済み注文の表示補正、お渡し日時のクラウド往復、会計状態を変更しないこと
- 後日受取の「受け取り時会計」自動選択、現金／クレジットの会計記録、会計前のお渡し禁止、会計日時のクラウド往復
- 会計記録の確定前・二重押下・通信失敗・別端末競合・同期中の状態変化に対する保護
- 単票・お客様控え・一括印刷のファイル名、お客様名の使用不可文字除去、日本時間の日時
- 注文区分に応じた確定時の状態、注文確定の表示、国内専用印刷の除去と全期間初期選択
- 日付欄の寸法・余白、クイック日付で画面を再作成しないこと、QRボタンの改行防止

## ブラウザ実操作

実商品・実顧客・実パスワードを使わず、架空商品を返すローカル環境でHeadless ChromeとWebKitを使用しました。

- 誤ったパスワードではログイン不可: PASS
- 正常ログイン後の品番検索・商品追加・注文入力・保存: PASS
- オフライン時は未保存表示、入力維持、再接続後の保存: PASS
- DB保存後の通信応答断を再現し、内容を修正して再試行: 重複なし、変更保持
- 印刷後に注文が残る: PASS
- 再読み込み、新しいタブでのログイン復元・注文復元: PASS
- セッション期限切れからの自動更新: PASS
- 別ブラウザ環境から保存済み注文を取得: PASS
- 別担当者の注文を編集しても担当者を保持: PASS
- 2端末の同時編集: 後から古い内容を保存する操作を拒否し、最新内容を再取得
- 非表示操作がもう一方へ自動同期される: PASS
- ログアウトで端末の認証情報を除去: PASS
- 1024px、390px、320px: 横はみ出しなし、不要な状態表示なし

## 3タブ・お客様控えPNG・QR

- 通常注文の確定後は完了で表示し、要対応・受け取り待ち・完了へ変更／復元できることを確認
- 別タブの注文も検索でき、タブを押すと通常の絞り込みへ戻ることを確認
- 実際の控えDOMからPNG生成し、架空のお客様名・注文商品・数量・金額を目視確認
- 社内備考・スタッフ名・帳合先は控え画像に含まれない
- ログインしていない別ブラウザでQRのリンクからPNGを取得できる
- トークンなし・改変トークン・別画像へのパス改変は拒否
- 同じ画像の再発行でもエラーにならず、保存済み注文を保持
- 実環境同等の長さの署名トークンを使い、1024px・390px・320pxでQRをデコードして元のURLと一致
- QR検証には一時フォルダのjsQR 1.4.0を使用。公開アプリには含めない

実Supabaseでも、顧客情報を含まない1ピクセルPNGで、スタッフのアップロード・署名URL発行・匿名の画像取得（HTTP 200）を確認しました。署名なし・公開バケットURL・改変トークン・匿名での署名発行はHTTP 400で拒否されました。検証用画像はStorage APIで削除し、検証用ログインだけをログアウトしました。注文行は変更していません。

Storageは非公開・PNGのみ・10MB上限・RLS有効。有効スタッフ限定のSELECT／INSERT／DELETEポリシーを設定し、UPDATEによる上書きは許可していません。検証後の画像行0件、現注文13件、旧注文22件を確認しています。

## 実データベースの権限検証

実テーブル上で有効スタッフの認証条件を再現し、作成・閲覧・更新・非表示・監査項目を確認しました。非スタッフは0件となることを確認後、トランザクション全体をROLLBACKしました。

新テーブルはスタッフ限定RLS、作成・変更可能な列の制限、DB側の監査時刻設定を備えます。物理DELETE権限は付与していません。

Security Advisorでは新テーブル・新関数への警告はありません。既存の旧関数・拡張・Auth設定に関する警告は残っています（移行報告書参照）。

## PDF・印刷（既存の検証結果）

前回の共有ボタン追加時に、PDFスキルの手順に沿い、実際の印刷DOMから架空注文のA4 PDFを生成し、PNGへレンダリングして目視確認しました。今回のお渡し対応では印刷レイアウトを変更していません。印刷操作と注文保持のブラウザ回帰テストは再実行しています。

- A4単票1ページ・共有ボタンの注文書1ページ・全注文一括3ページ（一覧＋通常注文＋後日受取）の全ページを確認
- ロゴ、注文番号、作成日時、顧客情報カード、商品表、単価、金額、合計を表示
- 備考・案内・通貨・フッターを表示
- 文字切れ、重なり、黒塗り、文字化けなし

検証用PDF・PNGは確認後に削除し、GitHubへ残していません。

## 今回の入力画面・共有チェックの追加検証

- 通常注文／その場渡し／後日受取／ホテル配送／指定先配送の5区分を作成
- Chrome系・WebKit系それぞれで320／375／390／430／768／1280pxの6幅を確認
- 入力欄・選択欄・日付・チェック・ボタン・詳細行が画面外へはみ出さないことを寸法で検証
- 受け取り予定日・チェックアウト日・印刷期間の日付は幅100%・高さ48px・左寄せ。予定日とクイックボタンの間隔、ボタン同士の幅と高さを確認
- 明日／明後日／任意の日付を切り替えても入力内容を保持し、画面を閉じて再開した場合も保持
- 確定画面と詳細画面の「お客様控え（QR・画像）」を1行で表示。長い配送先の折り返しも確認
- 通常注文・その場渡しの確定は完了。後日受取・配送は共有チェックなしの新規注文が要対応。チェックありの確定は後日受取が受け取り待ち、ホテル・指定住所配送が完了
- 詳細の共有チェックも同じ状態へ移動。未受取の後日受取は共有チェックだけで完了しない
- 共有状態と日時は別ブラウザへ自動同期し、再読み込み後も復元。通常編集で共有日時を上書きしない
- 通信失敗はチェックを元に戻し、クラウド側を変更しない。別端末と競合した共有変更は拒否
- 一括印刷は全区分・全状態・全期間を標準にし、一覧の検索条件に左右されない。異なる受付日の注文も含める
- 印刷後も架空注文5件を保持。Slackへ実際の投稿は行わない
- 実SupabaseのRLS有効、匿名SELECT不可、認証済みのpayload更新権限、物理DELETE不可を再確認。今回は本番注文を書き換えない

再現用: `tests/layout-sharing-browser-check.mjs`。`LAYOUT_BROWSER=webkit` でWebKit系、`LAYOUT_FIXTURE_URL` で独立した架空データサーバーを指定します。実機Safariや物理プリンターの検証ではありません。

## Slack共有ボタンの追加・対象限定

- 入力画面・詳細画面とも、後日受取／ホテル配送／指定住所配送だけ共有チェックと「共有」を表示。通常注文／その場渡しには両方とも出さない
- 320〜1280pxの6幅、Chrome系／WebKit系で、チェックと共有ボタンが横並び・文字折り返しなし・重なりなし。共有ボタンは高さ48px
- 必須欄が不足する場合は印刷せず案内。入力済みの場合は現在の注文内容を既存の注文書として出力
- 「共有」の操作だけでは共有チェック・注文のクラウド内容を変えない。入力と保存済み注文は印刷後も保持
- 印刷DOMとPDFに「未確定」「確認用」「登録前」を出さない。後日受取の印刷時の受付番号と、確定後の受付番号が一致
- PDF出力は既存のブラウザ印刷を使用。Slack自動送信・新しい外部サービス連携・権限変更は追加しない

## 後日受取のお渡し完了

- 注文一覧と詳細の両方に、未受取の後日受取だけ「お渡し済み」を表示。通常注文・その場渡し・ホテル配送・指定住所配送には表示しない
- 未受取の後日受取は、状態選択から直接「完了」にできない
- 旧仕様で共有だけで完了になった架空注文は、保存内容を書き換えずに受け取り待ちへ表示
- 一覧・詳細のお渡し操作で、クラウド保存成功後だけ完了へ移動。お渡し日時も保存し、会計状態は変更しない
- 通信失敗時は未受取のまま、ボタンを再操作できる。一覧ボタンの連打でも更新リクエストは1回
- 別ブラウザへお渡し状態・日時が自動同期し、再読み込み後も復元
- お渡し後のSlackチェック解除・再チェックでは完了を維持。状態を受け取り待ちへ戻すと、お渡し状態と日時を取り消す
- Chrome系・WebKit系の320／390／1280pxで詳細パネルを確認し、一覧ボタンも目視確認。横はみ出し・文字の不自然な折り返しなし、ボタンの高さ48px以上
- 実Supabaseは読み取り専用でRLS・匿名権限・payload更新権限を再確認。本番注文の一括変更や新しいDB権限の付与なし

## 未実施・人手確認

- 実顧客を使った本番画面操作や、実物のスマートフォンのカメラ操作は行っていません。実Storageの権限検証、架空データのブラウザ操作、画面サイズごとのQRデコードを分けて確認しました。
- OSの印刷ダイアログから物理プリンターへの出力は、使用端末とプリンターで確認してください。
- 旧Supabase注文API停止とGit履歴書き換えは、影響が大きいため明示承認前には実施していません。
# 2026-09-04 注文削除ボタン

- 完了注文カードに「削除」、全状態の詳細に「この注文を削除（キャンセル）」を追加。
- 確認には店舗・受付番号（ある場合）・金額・全端末への影響・共有済み控え等の注意を表示。
- ソフト削除と既存の更新日時照合を維持。物理削除権限・DBスキーマは変更なし。
- `node --test tests/*.test.mjs`：66件すべて成功（新規の削除回帰テスト7件を含む）。
- 追加検証：確認取消、連打防止、保存確認前の保持、通信失敗、競合時の再取得、他端末同期、古い同期応答による復活防止、削除中に別画面へ移動。
- `node --check app.js`、`node scripts/build-site.mjs`、`git diff --check`：成功。
- 架空データのローカル画面で1280px・320pxの削除ボタン配置を視認。ネイティブ確認ダイアログを開くと検証ブラウザの操作APIがタイムアウトしたため、ダイアログの確定・取消処理は上記自動テストで検証。本番注文の削除は行っていない。
