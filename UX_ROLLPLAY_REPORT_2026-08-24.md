# 初心者スタッフ向け UI ロールプレイ検証レポート

実施日: 2026-08-24  
対象: 展示会スタッフ専用 注文管理ツール  
結論: 基本の注文登録フローは分かりやすく、スマートフォンでも実務に使える水準です。ただし、展示会前のロールプレイへ入る前に「絞り込みと件数の矛盾」「日別提出と提出前チェックの不足」「入力途中の注文を破棄できない」「お客様控えQRを確実に発行できる運用」の4点は優先修正が必要です。

## 1. 検証条件

- 初心者スタッフが説明を受けずに触る想定
- 390 × 844 px の一般的なスマートフォン
- 320 × 568 px の小さめのスマートフォン
- 1024 × 768 px のPC・タブレット
- 端末モードで、通常注文と現売り・後日受取を実際に登録
- 本社未共有 → 受取待ち → 完了の操作を確認
- 検索、絞り込み、入力途中の復帰、印刷・PDF、QR導線を確認
- 入力不足、選択忘れ、途中で閉じる操作も確認
- 自動テスト 24/24 PASS、`workflow.js` と `app.js` の構文チェック PASS

検証用として端末モードに次の2件を作成しました。オンライン共有はされていません。

- `ロープレ確認店 8/24`：国内通常注文、完了
- `ロープレ後日受取店`：現売り・後日受取、本社共有済み、受取待ち

## 2. 総合評価

| 観点 | 評価 | コメント |
|---|---:|---|
| 初回の分かりやすさ | ○ | ログイン、注文管理、新しい注文の入口は明快 |
| 注文入力 | ○ | 3段階、商品完全一致の強調、固定入力キーが有効 |
| 状態管理 | △ | 状態名と次のボタンは良いが、絞り込みとの矛盾がある |
| 見やすさ | ○ | 横はみ出しなし。カードと主要ボタンは読みやすい |
| 混雑時の迷いにくさ | △ | 下書き破棄不可、検索範囲、タップ領域に改善余地 |
| 提出・締め作業 | × | 日付指定、未同期・未完了チェック、対象確認が不足 |
| スタッフ専用運用 | △ | ログイン運用は良いが、端末モードを選びやすすぎる |

## 3. 優先して直すべき問題

### P0: 件数があるのに押すと「0件」になる

再現できた事象:

1. `未会計 1` を押すと `要対応` タブへ移動する。
2. 未会計の注文が `受取待ち` にある場合、画面には「該当する注文はありません」と表示される。
3. `完了` タブを見ている状態で「本社未共有」を絞り込むと、要対応に対象があっても0件になる。
4. 本社共有済みにして注文が `受取待ち` へ移動した後も「本社未共有」の絞り込みが残り、移動先が空に見える。

初心者は「注文が消えた」「登録できていない」と判断しやすく、混雑時に再登録や二重登録へつながります。

推奨修正:

- 上部3件数を押したときは、タブに関係なく該当注文を一覧表示する。
- あるいは絞り込みに合うタブだけを自動選択する。ただし `未会計` は要対応・受取待ちの両方に存在するため、タブをまたぐ検索結果画面の方が安全。
- 状態更新後は、旧状態の絞り込みを自動解除する。
- 空表示を「受取待ちにはありません。要対応に1件あります」のようにするだけでも迷いを減らせる。

根拠となる実装箇所: `app.js` 173–179行、194行、200行、415行。

### P0: 最終提出が「全件をすぐ印刷」だけで、提出前の事故を防げない

現在の提出画面は次の2択です。

- 国内通常注文をまとめて印刷
- 展示会の全注文データをPDF・印刷

不足している確認:

- 受付日・日付範囲の指定
- 対象期間の表示
- 未同期件数
- 本社未共有件数
- 未会計・受取待ち・要対応件数
- 出力対象の件数と合計の最終確認
- 端末モードでは「全スタッフ分」ではなく「この端末分のみ」である警告

現状は、未完了や未同期の注文を含んだまま全注文PDFを提出でき、出力表紙にも対象受付日がありません。

推奨する最小構成:

1. ホームのボタン名を `提出・印刷` にする。
2. 用途は現状の2択を維持する。
3. その下に日付範囲を3択だけ追加する。
   - `本日`
   - `日付を指定`
   - `全期間`
4. 最後に1枚の確認枠を表示する。
   - 対象日
   - 注文数
   - 商品点数
   - 合計
   - 未同期 / 要対応 / 受取待ち / 本社未共有
5. 展示会終了後の最終提出では、未同期が1件以上なら出力を止める。未完了がある場合は強い警告と責任者確認を入れる。

運用中の `要対応` と `受取待ち` は日付で自動的に隠さないでください。前日の未処理を見落とすためです。日付絞り込みは主に `完了` と `提出・印刷` で使うのが安全です。

根拠となる実装箇所: `app.js` 327–333行、396–400行。

### P0: 入力途中の注文を破棄して別のお客様を受けられない

途中で閉じた注文は保持され、`入力途中の注文を再開` になる点は優れています。しかし、現在は下書きを破棄する操作がなく、新しいお客様の注文を開始できません。

展示会では「お客様が一度離れた」「品番確認待ちになった」「別スタッフが次のお客様を先に受ける」が起きます。混雑時に作業が止まる可能性があります。

推奨修正:

- 下書きがあるとき、下部に `入力途中を再開` を主ボタンで表示。
- その下に小さく `破棄して新しい注文` を表示。
- 破棄時だけ確認画面を出す。
- 将来複数下書きを増やすより、まず1件の再開・破棄だけに留める。

根拠となる実装箇所: `app.js` 204–211行。下書きを破棄する経路はありません。

### P0: 必須のお客様控えQRが端末モードでは発行できない

お客様へ控えを渡す際、QRコードは必須です。現在は端末モードでも登録完了画面に `お客様控えQR` が大きく表示されますが、押して初めて「登録スタッフでログインし、オンライン同期した注文で利用できます」と分かります。つまり、端末モードでは注文登録を完了できても、お客様への必須の控え渡しを完了できません。

また、端末モードの全注文PDFは、この端末にある注文だけです。スタッフ全員の注文を集約した最終提出には使えません。

推奨修正:

- 通常運用では登録完了後の主ボタンを `お客様控えQRを表示` とし、管理画面へ戻る操作より優先する。
- 注文保存だけでなく、QRの生成・別端末での読み取り確認までを「お客様対応完了」の条件として扱う。
- QR生成に失敗した場合は、登録済み注文を失わず `QRをもう一度作る`、`通信状態を確認`、`控え画像をこの端末で表示` の順に復旧できるようにする。
- ログイン画面の端末モードは `通信障害時の緊急モード` という小さな導線へ変更し、開始前に「他スタッフと共有されない」「通常のオンラインQRは発行できない」を確認表示する。
- QRを必須とするなら、端末モードでも発行できる緊急用QR方式を展示会前に用意する。候補は、控えの最小データをURLのフラグメントへ保持し、公開済みの静的な控え画面で表示する方式。個人情報、QR容量、改ざん、訂正後の再発行を事前検証する。
- 緊急用QRを安全に実装できない場合は、端末モードを通常受注に使わず、ログイン・同期の復旧を優先する。復旧までの暫定手段としてスタッフ端末上の控え画像または紙控えを用意する。
- 端末モードでは `展示会の全注文データ` を無効化するか、`この端末の注文だけを出力` と明記。
- 復旧後に未同期注文を一覧確認できる導線を設ける。

根拠となる実装箇所: `app.js` 384–394行、396–400行、407行。

## 4. 次に直したい問題

### P1: 検索は現在のタブ内だけだが、それが分からない

`受取待ち` で完了済みの店舗名を検索すると0件になります。検索欄は「店舗・受付番号・電話・品番」とだけ表示され、現在のタブだけが対象とは分かりません。

推奨:

- 検索結果は3タブ横断にする。
- 難しければ、検索欄を `このフォルダ内を検索` と明記し、0件時に他フォルダの一致件数を案内する。

### P1: 注文カードに受付日時がない

後日受取日がある注文以外は、いつ受けた注文か一覧で分かりません。日別提出を追加しても、画面上で対象を照合できません。

推奨:

- カード下部に `受付 10/06 14:32` を1行追加。
- 受取予定日は別項目として維持し、受付日と混同させない。
- 詳細画面にも作成日時・最終更新日時を表示。

### P1: 現売りの渡し方が自動で「その場渡し」になる

`現売り対応` を選ぶと、渡し方は明示操作なしで `在庫あり・その場渡し` が選択されます。そのまま進むと、会計・お渡し完了の登録になります。

誤登録の影響が大きいため、渡し方は初期未選択にして必ず1回選ばせる方が安全です。また `ホテルへ配送` と `指定住所へ配送` が両方 `3` になっており、後者は `4` が正しいです。

根拠となる実装箇所: `app.js` 249行。

### P1: 卸屋・帳合先の「その他」で名称を残せない

`その他` を選んでも名称入力欄が出ないため、提出データには「その他」としか残りません。

推奨:

- `その他` を選んだときだけ `卸屋・帳合先名` を必須表示。
- 機能追加はこの条件付き1項目だけにする。

### P1: 電話番号は空欄しか検査していない

文字列が1文字でも入れば登録できます。海外番号もあるため厳格な国内形式固定は避けつつ、数字・`+`・`-`・空白・括弧以外が含まれる場合は警告した方が安全です。

根拠となる実装箇所: `workflow.js` 42行。

### P1: 小さい端末では固定入力キーが押し間違いやすい

320 px幅で横はみ出しはありませんでした。一方、数字キーは高さ32 px、左右切替は27 pxでした。一般的なタップ目安より小さく、立ったまま・片手・混雑中では誤入力が増えます。

推奨:

- 数字キーは最低40 pxを目標にする。
- 左右切替は頻用しないため、現在の位置を保ちながらタップ領域だけ広げる。
- `A字` は `英字` または `ABC` に変更。

## 5. 軽微だが迷いやすい点

- 商品未追加でも `注文内容へ進む` が押せる。押した後のエラーは分かるが、0点ならボタンを無効にした方が早い。
- 注文方法未選択でも `入力へ進む` が押せる。同様に無効化できる。
- 商品未追加エラーが、商品追加後もしばらく残る。解決した時点で消す。
- 完了カードに `内容を見る` と `詳細` の2つがあり、違いがない。1つに統一する。
- 上段の件数とタブの両方に `受取待ち` があり、初心者には違いが分かりにくい。上段は `重要な確認` など見出しをつける。
- 登録完了後に `続けて新しい注文` がない。管理画面へ戻る一手が必要。
- `更新` が記号だけで、何を更新するか分かりにくい。長押し説明ではなく、同期時刻の近くに `同期` と表示するとよい。

## 6. 良かった点

- ログイン画面は要素が少なく、目的が明確。
- 端末モード中は赤点と「共有されません」が常時表示される。
- 新規注文は `商品 → 注文方法 → 入力・確認` の3段階で理解しやすい。
- 品番完全一致が緑で強調され、似た品番との区別がしやすい。
- 商品追加後も検索候補を保ち、同系統商品を続けて追加できる。
- 商品数、合計、数量変更がその場で分かる。
- 入力途中で閉じても内容を保持し、再開できる。
- `本社未共有で要対応に登録`、`共有済みで受取待ちに登録` のように、ボタン名だけで結果が分かる。
- 本社共有前に印刷・PDF導線と確認画面がある。
- 会計・お渡し完了は確認画面を挟む。
- 320 px幅でも横スクロールは発生しない。
- 赤・緑だけに頼らず「未共有」「共有済み」と文字でも示している。

## 7. 推奨するシンプルな完成形

ホーム画面は大きく増やさず、次の構造を推奨します。

1. 上部: `本社未共有 / 受取待ち / 未会計`
2. 中段: `要対応 / 受取待ち / 完了`
3. 検索: 3フォルダ横断
4. 絞り込み: `担当者 / 注文区分 / 受付日` を必要時だけ開く
5. `提出・印刷`
6. 注文一覧
7. `新しい注文`

常時見せるものを増やさず、絞り込みと提出の中でだけ日付指定を見せるのが、「シンプルだが、かゆいところに手が届く」構成です。

## 8. 展示会前ロールプレイ項目

### 基本操作

- 新人が説明なしで通常注文を1件登録できる
- 新人が現売り4種類を正しく選べる
- 同じ品番の別商品を取り違えない
- 商品数量を修正し、合計を確認できる
- 入力途中で閉じて再開できる
- 下書きを破棄して別のお客様を受けられる
- 登録完了後、スタッフが迷わずお客様控えQRを表示できる
- お客様のスマートフォンでQRを読み取り、控え画像を保存できる
- 控えに本社共有・社内状態などの内部情報が表示されない
- 注文訂正後にQRから最新の控えを確認できる、または再発行が必要だと明示される

### 状態管理

- 本社未共有から受取待ちへ移した注文が、移動直後に画面上で見える
- `未会計 1` を押すと必ず1件見える
- 受取完了後、完了へ移した注文が見える
- 完了後に修正し、全端末へ反映される
- 同じ注文を2端末で同時に開いた場合の扱いを確認する

### 通信障害

- 入力途中で通信を切る
- 未同期表示を新人が見つけられる
- 通信復旧後に同期完了を確認できる
- 端末モードの注文を最終提出へ混ぜない
- 別端末から同じ注文が見えるまでの時間を確認する
- 通信障害中でも、定めた緊急手順でお客様へ控えを渡せる
- QR生成失敗後に注文を二重登録せず、同じ注文から再発行できる

### 日別提出

- 23:59と00:01に作成した注文が日本時間の別日になる
- `本日`、指定日、全期間の件数と合計が一致する
- 受付日と受取予定日を混同しない
- 未同期が1件あると最終提出を止められる
- 要対応・受取待ちが残る場合に警告される
- PDF表紙に展示会名、対象日、出力日時、件数、点数、合計が出る
- PDFの一覧と明細の件数が一致する

### 合格条件

- 件数表示と一覧件数が矛盾しない
- 状態変更した注文が消えたように見えない
- 新人が5分以内に通常注文、7分以内に後日受取を登録できる
- 誤タップによる渡し方の間違いが0件
- 日別PDFの対象件数・点数・金額が元注文と一致する
- 未同期注文を含んだ最終提出を作れない
- 操作後に「次に何をすべきか分からない」が0回
- オンライン注文のQR発行成功率が100%で、失敗時も同じ注文から復旧できる

## 9. 修正順序

1. 絞り込み・タブ・件数の矛盾を修正
2. 状態更新後に旧絞り込みを解除
3. 下書きの破棄と新規開始を追加
4. 提出画面に日付範囲と提出前チェックを追加
5. お客様控えQRを必須フロー化し、端末モード用の緊急発行方法を用意
6. 現売りの渡し方を明示選択にし、番号を修正
7. 受付日時、横断検索、その他卸屋名を追加
8. タップ領域と軽微な文言を調整
