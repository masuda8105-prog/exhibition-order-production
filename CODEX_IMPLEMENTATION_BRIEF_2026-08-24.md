# Codex実装指示書 — 展示会注文管理 UI改善

作成日: 2026-08-24  
対象リポジトリ: `C:\Users\AONUSR02\Desktop\exhibition-order-production`  
参照レポート: `UX_ROLLPLAY_REPORT_2026-08-24.md`

## Codexへの依頼

参照レポートを最初から最後まで読み、本指示書を優先仕様として、初心者スタッフが展示会本番で迷わず使えるように実装・テストしてください。調査や提案だけで終了せず、コード修正、単一HTML版の再生成、自動テスト、スマートフォンでの実操作確認まで完了させてください。

今回の重要な決定事項は次のとおりです。

- このツールを使うのは登録スタッフのみ。
- お客様控えQRは補助機能ではなく、控えを渡すための必須機能。
- 本番画面から匿名の「端末モード」は削除する。
- 通信障害時は、ログイン済みスタッフの注文を端末へ一時保存し、復旧後に同期する。
- UIはシンプルなまま維持し、日付指定などは必要な画面の中だけに置く。
- 既存の良い商品検索、3段階入力、状態管理、印刷レイアウトは壊さない。

## 1. 目的

次の事故を防ぐことが目的です。

- 件数が表示されているのに、押すと0件になって注文を見失う。
- 状態変更後、移動した注文が消えたように見える。
- 入力途中の注文が残り、別のお客様の新規注文を始められない。
- 必須のお客様控えQRを発行できないまま対応が終了する。
- 未同期・未完了の注文を含んだまま最終PDFを提出する。
- 日付別に抽出できず、日次締めと最終提出の対象が曖昧になる。

## 2. 実装の優先順位

以下の順に実装してください。後の項目のために前の項目を壊さないでください。

1. 件数・タブ・絞り込みの矛盾解消
2. 本番の端末モード削除とログイン済みオフライン保存の維持
3. お客様控えQRの必須フロー化
4. 入力途中注文の破棄と新規開始
5. 日付別の提出・印刷と提出前チェック
6. 横断検索、受付日時、現売り選択、その他卸屋名
7. タップ領域、文言、不要な重複操作の調整

## 3. 必須実装

### 3.1 件数・タブ・絞り込みを矛盾させない

現状の問題:

- `未会計 1` を押しても、未会計注文が受取待ちにあると0件になる。
- 完了タブ上で「本社未共有」を選ぶと、要対応に対象があっても0件になる。
- 本社共有後に受取待ちへ移動しても、旧フィルターが残って0件に見える。

要件:

- 上部の `本社未共有 / 受取待ち / 未会計` は、現在のタブに制限されず、該当注文をすべて表示する。
- 上部件数を押した結果の表示件数は、件数カードの数字と必ず一致させる。
- 絞り込み画面から同じ条件を選んだ場合も、現在のタブに制限しない。
- 絞り込み中は、タブのどれかが選択中に見えることで誤解を生まないよう、`絞り込み結果` と分かる表示にする。
- タブを押したら絞り込みを解除し、そのフォルダだけを表示する。
- 注文を別状態へ移した直後は、移動先の注文が必ず画面に見えるよう、矛盾するフィルターを解除する。
- 空表示には、別フォルダに一致注文がある場合の案内を出す。

合格例:

- 未会計件数が1なら、`未会計 1` を押した一覧も1件。
- 本社共有済みにして受取待ちへ移した直後、その注文カードが表示される。
- 会計・お渡し完了後、完了へ移した注文カードが表示される。

### 3.2 本番画面から端末モードを削除する

要件:

- ログイン画面の `端末モード（共有されません）` ボタンを本番UIから削除する。
- `onlineEnabled: true` では、登録スタッフのログイン以外に注文管理へ入る方法を出さない。
- 既存のローカル注文や未同期注文を削除しない。
- ログイン後に通信が切れた場合は、現在の端末保存と未同期キューを維持する。
- 通信復旧時、既存の `syncPendingOrders()` 相当で自動同期する。
- 未同期件数が0になったことをスタッフが画面上で確認できるようにする。
- ログイン前の完全オフライン状態では、匿名受注画面へ進ませず、再接続が必要だと分かる案内を表示する。
- 開発確認用に `onlineEnabled: false` の内部経路を残す場合も、本番設定では絶対に表示されない構造にする。開発用経路を「端末モード」として通常ユーザーへ見せない。

注意:

- 既存の端末保存データを削除する移行処理は作らない。
- 以前端末モードで作られた未同期注文がある場合、スタッフログイン後に安全に同期対象へ入る既存挙動を維持する。

### 3.3 お客様控えQRを必須フローにする

業務上の前提:

- QRコードは、お客様へ注文控えを渡すために必要。
- お客様対応は、注文を保存しただけでは完了ではない。QRを表示し、お客様が読み取れる状態まで必要。

要件:

- 登録完了画面では `お客様控えQRを表示` を主ボタンにする。
- `管理画面へ戻る` は副ボタンにする。
- オンライン登録時は、注文保存後に公開トークンが取得できているか確認する。
- QR準備中、準備完了、失敗を明確に表示する。
- 失敗しても注文を二重登録させない。同じ保存済み注文から `QRをもう一度作る` で再試行する。
- QR失敗時に、注文番号・店舗名・保存済みであることを表示する。
- QRから開くお客様控えは、注文修正後も同じ公開トークンで最新内容を表示する。
- お客様控えには、本社共有、要対応・受取待ち・完了、社内案内、社内担当用情報を表示しない。
- お客様名の敬称重複防止、画像長押し保存、スマホ横はみ出しなしを維持する。
- QR表示画面から閉じた後も、注文詳細から再表示できる。

通信障害時:

- 注文は端末へ保存し、`注文は保存済み・QRは未発行` と区別して表示する。
- QRが必須なので、QR未発行を「対応完了」と誤認させない。
- この実装で安全な緊急用QRまで実現できない場合、無理なオフラインQR方式を追加しない。代わりに、接続復旧後に同じ注文から確実にQRを発行できる復旧導線を完成させる。
- 緊急用QRを追加する場合は、個人情報、QR容量、改ざん、訂正後の再発行、別端末読み取りをテストし、根拠を説明する。

### 3.4 入力途中の注文を破棄できるようにする

現状の良い点である「閉じても再開できる」は維持してください。

要件:

- 下書きがある場合、主ボタンは `入力途中の注文を再開`。
- その近くに、目立ちすぎない `破棄して新しい注文` を追加。
- 破棄時は確認画面を出す。
- 確認文に、追加済み商品数と入力済み店舗名があれば表示する。
- キャンセルすれば元の下書きへ戻れる。
- 破棄を確定した場合だけ `state.draft` 相当を消し、新しい注文を開始する。
- 注文編集時のデータや保存済み注文を破棄対象にしない。

### 3.5 日付別の提出・印刷を追加する

ホームの `印刷・PDF` は `提出・印刷` へ変更してください。

用途は増やしすぎず、現在の2種類を維持します。

- 国内通常注文をまとめて印刷
- 展示会の全注文データをPDF・印刷

各用途を選んだ後に、対象受付日を次の3択で指定できるようにします。

- `本日`
- `日付を指定`
- `全期間`

日付要件:

- 判定基準は受取予定日ではなく注文の `createdAt` / サーバーの `created_at`。
- 日本時間 `Asia/Tokyo` の日付で判定する。
- 指定日は開始・終了を含む。
- 23:59と00:01が正しく別日に分かれること。
- 日付不明の古い注文は全期間では表示し、日付指定から黙って混入させない。

出力前確認:

- 対象日または対象期間
- 注文数
- 商品点数
- 合計金額
- 未同期件数
- 本社未共有件数
- 要対応件数
- 受取待ち件数
- 未会計件数

制御:

- 未同期が1件以上ある場合、展示会の最終提出用PDFは出力させない。
- 要対応・受取待ち・本社未共有がある場合、強い警告を出す。
- 警告後に出力を許可するか止めるかは、通常注文の伝票打ち用と展示会最終提出用で分ける。
- 個別注文の本社共有用印刷は、この一括提出制御の影響を受けず利用できること。
- PDF表紙へ展示会名、対象受付日、出力日時、注文数、点数、合計を表示する。
- 一覧にも受付日時を載せ、対象日の照合ができるようにする。

重要:

- `要対応` と `受取待ち` の通常運用一覧は日付で自動的に隠さない。前日以前の未処理を見落とさないため。

## 4. 併せて実装する改善

### 4.1 検索を3フォルダ横断にする

- 店舗、受付番号、電話、品番、商品名を全注文から検索する。
- 検索結果カードに `要対応 / 受取待ち / 完了` を明記する。
- 検索中は `検索結果` と表示し、タブ内検索だと誤解させない。
- 検索を解除すると元のタブへ戻る。

### 4.2 注文カードと詳細へ受付日時を表示する

- カードに `受付 10/06 14:32` を追加。
- 詳細に作成日時と最終更新日時を追加。
- 受取予定日とは別の項目として表示する。

### 4.3 現売りの渡し方を必ず明示選択させる

- `現売り対応` 選択時に `その場渡し` を自動選択しない。
- 渡し方が未選択なら次へ進めない。
- 番号を次へ統一する。
  1. 在庫あり・その場渡し
  2. 翌日・翌々日に受取
  3. ホテルへ配送
  4. 指定住所へ配送

### 4.4 卸屋・帳合先の「その他」を具体名で保存する

- `その他` 選択時だけ `卸屋・帳合先名` を必須表示。
- 一覧、詳細、印刷、並び替えには入力された具体名を使う。
- 既存注文との後方互換を保つ。
- 既存の候補外の名称を編集した場合も「その他＋具体名」として正しく復元する。

### 4.5 電話番号の入力ミスを警告する

- 海外番号を許容する。
- 数字、`+`、`-`、空白、括弧以外を含む場合は警告する。
- 厳格すぎる国内電話番号固定にはしない。
- 入力値を勝手に変更しない。

### 4.6 小さい端末のタップ領域を改善する

- 320px幅で横はみ出しを発生させない。
- 固定数字キーは可能な限り高さ40px以上。
- 左右位置切替は見た目を増やさず、タップ可能領域を広げる。
- `A字` を `英字` または `ABC` に変更。
- 主要ボタンは44px以上を維持する。

### 4.7 軽微な迷いを減らす

- 商品0点なら `注文内容へ進む` を無効化。
- 注文方法未選択、現売りの渡し方未選択なら `入力へ進む` を無効化。
- エラー原因を解消したら古いエラー表示を消す。
- 完了カードの `内容を見る` と `詳細` を1つに統一。
- 登録完了画面に `続けて新しい注文` を追加。ただしQR主ボタンより目立たせない。
- 更新記号だけでなく、同期中・最終同期時刻・未同期件数を分かる言葉で表示。

## 5. 変更してはいけないもの

- `product_master.csv` の商品、価格、注文可否
- 価格未定商品の追加禁止
- 重複品番を別商品として扱う仕様
- 国内通常注文、現売り4種の業務状態ルール
- 本社共有後の受取待ち・完了への遷移ルール
- Slackへ自動送信しない方針
- お客様控えから社内情報を除外する方針
- Supabaseの秘密情報をクライアントへ追加しないこと
- 既存注文、既存ローカルデータ、公開トークンを削除しないこと
- 無関係なExcelファイルやユーザーの未追跡ファイル

大規模な見た目の作り直し、メニュー階層の増加、不要なダッシュボード機能追加は行わないでください。

## 6. 後方互換・データ保護

- 新しいフィールドがない既存注文も表示・編集・印刷できること。
- 修正保存で既存の `localId`、`remoteId`、`publicToken`、注文番号を維持すること。
- 注文修正後も既存QRのURLを維持し、最新データを表示すること。
- 未同期注文はログイン後に同期し、重複注文を作らないこと。
- 日付フィルターは `createdAt` とサーバー `created_at` の双方を正規化して扱うこと。
- 既存の単一HTML版と通常版の機能差を作らないこと。

## 7. 必須テスト

### 自動テスト

既存24件をすべて維持し、次の回帰テストを追加してください。

- 上部件数と一覧件数が一致する。
- 未会計注文が要対応と受取待ちの両方にあっても全件表示される。
- 状態変更後に矛盾するフィルターが解除される。
- 下書きの再開、破棄キャンセル、破棄確定。
- 現売りの渡し方が初期未選択。
- ホテル配送3、指定住所配送4。
- その他卸屋名の必須検査と保存・復元。
- 電話番号の許容文字と警告。
- 日本時間の日付境界、23:59と00:01。
- 本日、指定日、全期間の抽出件数・点数・金額。
- 未同期がある場合の最終提出停止。
- QR再試行で新規注文を重複作成しない。
- 注文修正後も公開トークンを維持する。
- お客様控えに内部状態を表示しない。
- 本番設定で端末モードの入口が存在しない。

実行:

```powershell
node --test tests/*.test.mjs
node --check workflow.js
node --check app.js
node scripts/build-preview.mjs
```

### ブラウザ実操作

少なくとも次の幅で確認してください。

- 390 × 844
- 320 × 568
- 1024 × 768

必須シナリオ:

1. 登録スタッフログイン
2. 通常注文登録 → QR表示 → 完了一覧
3. 現売り・その場渡し → QR表示
4. 後日受取 → 本社未共有 → 共有済み → 受取待ち → お渡し完了
5. ホテル配送と指定住所配送
6. 未会計件数から対象全件を表示
7. 検索で3フォルダの注文を発見
8. 下書きを閉じて再開
9. 下書きを破棄して新規注文
10. 本日、指定日、全期間の提出対象確認
11. 未同期がある状態で最終提出を止める
12. QR失敗後、同じ注文から再試行
13. 注文修正後、同じQRから最新控えを表示

確認事項:

- 横スクロールなし
- 主要ボタンが画面外へ隠れない
- コンソールエラーなし
- 状態変更した注文が画面から消えたように見えない
- QRから開く控え画像をスマートフォンへ保存できる
- 通常版と単一HTML版の表示・動作が一致する

## 8. 完了条件

次をすべて満たした場合だけ完了としてください。

- 本番ログイン画面に端末モードがない。
- ログイン済み通信障害時の注文が端末保存され、復旧後に同期される。
- QRが登録完了後の主導線になっている。
- QR失敗時に注文を二重登録せず復旧できる。
- 件数表示と一覧件数が矛盾しない。
- 状態変更後の注文が移動先で見える。
- 下書きを再開または破棄して新規開始できる。
- 日付別に提出対象を抽出できる。
- 未同期注文を含む最終提出を作れない。
- 受付日時がカード・詳細・提出一覧で確認できる。
- 320px幅で横はみ出しがない。
- 既存テストと追加テストが全件合格する。
- `exhibition_order_production_preview.html` を再生成し、通常版と一致させる。
- `README.md` と `TEST_REPORT.md` を新仕様へ更新する。

## 9. Codexの最終報告に含めるもの

- 実装した変更の要約
- 変更したファイル一覧
- 自動テスト結果
- ブラウザで実行したロールプレイ結果
- QRの成功・失敗・再試行の結果
- 日付抽出の境界テスト結果
- 未同期提出防止の結果
- 実装できなかった項目と理由
- 本番前に人が確認すべき残事項

不明点があっても、安全で可逆な範囲は合理的に判断して実装を進めてください。業務ルール、データ削除、QRの安全性に影響する判断だけは、勝手に仕様を広げず明示してください。

